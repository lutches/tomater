     0       1    
→ s0 {s0 s1} {s2} 
  s1 {fail}  {s0}
* s2 {s2}    {s2}
  fail {fail} {fail}

#(0+01*)*(ε+1)1(ε+0+1)* looking at the regular expression we can see that the language can contain a number of 0's or (01)'s 
#then it can contain a 1 or not, then it must contain a 1, after this whatever input it will succeed


